#lang info

(define collection "layout")
(define deps
  (list "draw-lib"
        "pict-lib"
        "reprovide-lang-lib"))

(define build-deps '("rackunit-chk"))

(define pkg-authors '("schlee.simon@gmail.com"))
